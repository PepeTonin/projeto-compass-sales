import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  text: {
    fontFamily: 'Roboto-Medium500',
    fontSize: 16,
    marginRight: 8,
  },
});
