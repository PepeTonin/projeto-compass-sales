import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputsContainer: {
    width: '100%',
    marginTop: 40,
  },
  textContainer: {
    width: '100%',
    marginTop: 14,
    alignItems: 'flex-end',
  },
  buttonContainer: {
    width: '100%',
    marginTop: 32,
  },
});